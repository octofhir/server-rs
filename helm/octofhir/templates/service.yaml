apiVersion: v1
kind: Service
metadata:
  name: {{ include "octofhir.fullname" . }}
  labels:
    {{- include "octofhir.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "octofhir.selectorLabels" . | nindent 4 }}
