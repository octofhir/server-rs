[workspace]
resolver = "3"
members = [
    "crates/octofhir-core",
    "crates/octofhir-api", 
    "crates/octofhir-db",
    "crates/octofhir-search",
    "crates/octofhir-server",
]

[workspace.package]
version = "0.0.1"
edition = "2024"
license = "MIT OR Apache-2.0"
authors = ["OctoFHIR Team <funyloony@gmail.com>"]
repository = "https://github.com/octofhir/server-rs"
keywords = ["fhir", "healthcare", "server", "api"]
categories = ["web-programming", "science"]

[workspace.dependencies]
# Local dependencies
octofhir-core = { path = "crates/octofhir-core" }
octofhir-db = { path = "crates/octofhir-db" }
octofhir-canonical-manager = { path = "../canonical-manager" }
octofhir-fhir-model = { path = "../fhir-model-rs" }

# Test utilities
filetime = "0.2"

# Core dependencies
tokio = { version = "1.0", features = ["full"] }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
thiserror = "2.0"
anyhow = "1.0"

# HTTP server
axum = { version = "0.8", features = ["json",  "macros"] }
tower = { version = "0.5", features = ["timeout", "load-shed", "limit"] }
tower-http = { version = "0.6", features = ["trace", "cors", "compression-gzip"] }
hyper = { version = "1.0", features = ["full"] }

# Storage and concurrency
papaya = "0.2"
dashmap = "6.1"  # For some auxiliary maps if needed
uuid = { version = "1.0", features = ["v4", "serde"] }

# Time handling
time = { version = "0.3", features = ["serde", "macros", "parsing", "formatting"] }

# Configuration
config = "0.15"
toml = "0.9"
notify = "8"
notify-debouncer-mini = "0.7"

# Logging and observability  
tracing = "0.1"
tracing-subscriber = { version = "0.3", features = ["env-filter", "fmt", "registry"] }
opentelemetry = { version = "0.30", features = ["trace"] }
opentelemetry-otlp = { version = "0.30", features = ["http-proto", "reqwest"] }
tracing-opentelemetry = "0.31"
opentelemetry_sdk = { version = "0.30", features = ["rt-tokio"] }

# Utilities
futures-util = "0.3"
regex = "1.0"
url = { version = "2.5", features = ["serde"] }
indexmap = "2.0"
async-trait = "0.1"

# Development dependencies
tokio-test = "0.4"
tempfile = "3.0"
wiremock = "0.6"
assert-json-diff = "2.0"

[profile.dev]
incremental = true
debug = 2

[profile.release]
lto = true
codegen-units = 1
panic = "abort"

[profile.bench]
inherits = "release"
debug = true